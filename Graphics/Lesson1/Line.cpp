#include <bits/stdc++.h>
#include <graphics.h>

using namespace std;

void DrawLine1(int x1, int y1, int x2, int y2, int color){
	
}

int main(){
	int gd = DETECT, gm;
	initgraph(&gd, &gm, NULL);
	line(20, 50, 100, 90);
	getch();
	closegraph();
}
